name: Flutter CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build_and_test:
    name: Build & Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.x'
      - name: Get dependencies
        run: flutter pub get
      - name: Analyze code
        run: flutter analyze --no-fatal-infos
      - name: Run tests
        run: flutter test --dart-define=TMDB_TOKEN=${{ secrets.TMDB_TOKEN }}

  build_and_release_android:
    name: Build & Release Android
    needs: build_and_test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.x'
      - name: Get dependencies
        run: flutter pub get
      - name: Configure signing
        if: ${{ env.MOBMOVIZZ_KEYSTORE != '' }}
        env:
          MOBMOVIZZ_KEYSTORE: ${{ secrets.MOBMOVIZZ_KEYSTORE }}
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: |
          echo "$MOBMOVIZZ_KEYSTORE" | base64 --decode --ignore-garbage > android/app/keystore.jks
          echo "storePassword=$KEYSTORE_PASSWORD" > android/key.properties
          echo "keyPassword=$KEY_PASSWORD" >> android/key.properties
          echo "keyAlias=$KEY_ALIAS" >> android/key.properties
          echo "storeFile=keystore.jks" >> android/key.properties
      - name: Build APK
        run: flutter build apk --release --dart-define=TMDB_TOKEN=${{ secrets.TMDB_TOKEN }}
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: release-apk
          path: build/app/outputs/flutter-apk/app-release.apk

#  build_and_release_ios:
#    name: Build & Release iOS
#    needs: build_and_test
#    runs-on: macos-latest
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v4
#      - name: Set up Flutter
#        uses: subosito/flutter-action@v2
#        with:
#          flutter-version: '3.x'
#      - name: Get dependencies
#        run: flutter pub get
#      - name: Build and sign IPA
#        uses: apple-actions/import-codesign-certs@v1
#        with:
#          p12-file-base64: ${{ secrets.IOS_P12_BASE64 }}
#          p12-password: ${{ secrets.IOS_P12_PASSWORD }}
#      - run: flutter build ios --release
#      - name: Upload IPA
#        uses: actions/upload-artifact@v4
#        with:
#          name: release-ipa
#          path: build/ios/iphoneos/Runner.app
